/**
 * @function successReducer
 * @param {object} state - Success state contains array of guessed words.
 * @param {object} action - action to be reduced.
 * @returns {object} - new success state
 */
export default (state, action) => {
  return null;
}